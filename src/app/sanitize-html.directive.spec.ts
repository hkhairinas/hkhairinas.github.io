import { SanitizeHtmlDirective } from './sanitize-html.directive';

describe('SanitizeHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new SanitizeHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
